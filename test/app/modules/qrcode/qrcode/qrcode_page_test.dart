import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:ea_assistant/app/modules/qrcode/qrcode/qrcode_page.dart';

main() {
  testWidgets('QrcodePage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(QrcodePage(title: 'Qrcode')));
    //  final titleFinder = find.text('Qrcode');
    //  expect(titleFinder, findsOneWidget);
  });
}
