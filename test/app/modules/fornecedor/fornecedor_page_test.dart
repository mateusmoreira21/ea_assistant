import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:ea_assistant/app/modules/fornecedor/fornecedor_page.dart';

main() {
  testWidgets('FornecedorPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FornecedorPage(title: 'Fornecedor')));
    //  final titleFinder = find.text('Fornecedor');
    //  expect(titleFinder, findsOneWidget);
  });
}
