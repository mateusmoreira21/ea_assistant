import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ea_assistant/app/modules/login/reset/reset_controller.dart';
import 'package:ea_assistant/app/modules/login/reset/reset_module.dart';

void main() {
  initModule(ResetModule());
  // ResetController reset;
  //
  setUp(() {
    //     reset = ResetModule.to.get<ResetController>();
  });

  group('ResetController Test', () {
    //   test("First Test", () {
    //     expect(reset, isInstanceOf<ResetController>());
    //   });

    //   test("Set Value", () {
    //     expect(reset.value, equals(0));
    //     reset.increment();
    //     expect(reset.value, equals(1));
    //   });
  });
}
