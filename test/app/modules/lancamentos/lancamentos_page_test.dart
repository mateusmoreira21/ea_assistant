import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:ea_assistant/app/modules/lancamentos/lancamentos_page.dart';

main() {
  testWidgets('LancamentosPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(LancamentosPage(title: 'Lancamentos')));
    //  final titleFinder = find.text('Lancamentos');
    //  expect(titleFinder, findsOneWidget);
  });
}
